---
deployment:
  tasks:
    - export DEPLOYPATH=$HOME/public_html/expressbazar/
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH && /usr/local/bin/php artisan migrate --force
    - cd $DEPLOYPATH && /usr/local/bin/php artisan optimize:clear
