---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/whyceffy/expressbazar
    - mkdir -p "$DEPLOYPATH"
    - /bin/rsync -a --delete --exclude=".git" ./ "$DEPLOYPATH"/
    - cd "$DEPLOYPATH" && php artisan optimize:clear
