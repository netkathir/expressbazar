---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/whyceffy/public_html/expressbazar/

    - /bin/mkdir -p $DEPLOYPATH

    - /usr/bin/rsync -av --delete $HOME/repositories/expressbazar/ $DEPLOYPATH

    - cd $DEPLOYPATH

    - /usr/local/bin/php artisan optimize:clear