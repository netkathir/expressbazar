---
deployment:
  tasks:
    - export DEPLOYPATH=/home2/whyceffy/expressbazar
    - mkdir -p "$DEPLOYPATH"
    - find . -mindepth 1 -maxdepth 1 -not -name ".git" -exec /bin/cp -R {} "$DEPLOYPATH"/ \;
    - cd "$DEPLOYPATH" && php artisan optimize:clear
